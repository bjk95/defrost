# Concepts

Background reading. These pages explain how defrost works and why it's built
the way it is — useful before you wire it into a real project.

## Topics

_To be written._ Planned concept pages:

- **Git as the database** — why defrost stores history in commits instead
  of a service, and the trade-offs.
- **The `_defrost` branch** — lifecycle, what gets committed, and how to
  inspect it by hand.
- **OpenTelemetry as the ingestion API** — why defrost speaks OTel rather
  than shipping a client library.
- **Suppression model** — how suppressed tests differ from skipped ones,
  and why.
